#include<stdio.h>
void main()
{
    int sum=0,n,m;
    printf("Enter a number:");
    scanf("%d",&n);
    while(n!=0)
    {
        m=n%10;
        m=pow(1,digit)
        sum=sum+m;
        temp=temp/10;
    }
    if(sum==n)
    {
        printf("Armstrong number");
    }
    else
    {
        printf("Not a Armstrong number");
    }
}
